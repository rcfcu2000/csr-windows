<UserControl
    x:Class="csr_windows.Client.Views.Chat.ChatEndConversationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:csr_windows.Client.Views.Chat"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:csr_windows.Client.ViewModels.Chat"
    d:DataContext="{d:DesignInstance vm:ChatEndConversationViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="0,0,0,15">
        <StackPanel>
            <TextBlock
                Margin="0,0,0,10"
                FontSize="14"
                Text="如果顾客已下单，或您已经判断顾客不会再有更多咨询，可选择结束对话，我将不再给您催单建议。"
                TextWrapping="Wrap" />
            <Button
                Width="320"
                Height="40"
                hc:BorderElement.CornerRadius="5"
                Background="{StaticResource 0.1OpacityBlueTheme}"
                Command="{Binding EndConversationCommand}"
                Content="结束对话"
                Cursor="Hand"
                FontSize="14"
                FontWeight="Bold"
                Foreground="#FF2335C5" />
        </StackPanel>
    </Grid>
</UserControl>
